<div class="masthead clearfix md-header">
    <div class="inner">
        <div class="md-header-logo">
            <a href="{{ url('homepage') }}">
                <img src="{{ asset('images/logo.png') }}" class="header-logo" alt="Midiano" />
                <h3 class="masthead-brand md-header-brand">Midiano</h3>
            </a>
        </div>
        <nav>
            <ul class="nav masthead-nav md-header-nav">
{#                 <li><a href="#">Request</a></li>
                <li><a href="#">Submit</a></li> #}
{#                 <li>
                    <form class="header-search-bar search-submit">
                        <input type="search" name="q" id="search-query-input" value="" placeholder="Search..." class="extended">
                        <button><i class="fa fa-search"></i></button>
                    </form>
                </li> #}
                {# <li><a href="#">Search</a></li> #}
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    {# {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} | #}
                    {# <a href="{{ path('fos_user_security_logout') }}"> #}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <img src="{{ gravatar(app.user.email) }}" class="gravatar-round" />
                        </a>
                        <ul class="dropdown-menu dropdown-menu-sw dropdown-user-menu">
                            <div class=" dropdown-header header-nav-current-user css-truncate">
                            {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }}
                            </div>
                            <li role="separator" class="divider"></li>
                            {% if is_granted("ROLE_ADMIN") %}
                                <li><a href="{{ path('fos_user_security_logout') }}">Admin</a></li>
                            {% endif %}
                            <li><a href="{{ path('fos_user_profile_show') }}">Profile</a></li>
                            <li><a href="#">Favorites</a></li>
                            <li role="separator" class="divider"></li>
                            <li>
                                <a href="{{ path('fos_user_security_logout') }}">
                                    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path("fos_user_security_login") }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    </li>
                {% endif %}
            </ul>
        </nav>
    </div>
</div>
<hr />